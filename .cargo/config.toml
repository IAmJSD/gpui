# getrandom 0.3 requires the backend to be selected explicitly on
# wasm32-unknown-unknown; "wasm_js" uses the browser's crypto API. This pairs
# with the wasm32-only `getrandom` dependency in Cargo.toml.
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
